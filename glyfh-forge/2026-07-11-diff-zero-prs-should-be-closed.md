# 差分ゼロのPRは閉じる

## はじめに

PRの内容が別のPRで先に `main` に入ることがある。

その後にブランチを `main` へ追従させると、差分がゼロになる。

## どう見るか

GitHub上で `changed_files: 0` になっているなら、もうマージする変更はない。

この状態で無理に空の変更を足す必要はない。

## 今回の判断

#9〜#13は #8 に内容が含まれていた。

だから、英語コメントを残してcloseした。

## まとめ

だから、英語コメントを残してcloseした。
