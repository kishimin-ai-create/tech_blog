# アイコンだけ折り返しを変える

Xのプロフィールアイコンは正方形で小さい。

背景画像と同じ折り返しにすると、frameの形が潰れやすい。

## 今回の対応

アイコンでは、`frame_text` の最大文字数を2文字に制限する。

背景画像では、通常の `max_chars_per_line` を使う。

## なぜ分けるか

出力サイズが違うから。

同じルールを無理に当てるより、表示先ごとに読みやすさを守る方がよい。

