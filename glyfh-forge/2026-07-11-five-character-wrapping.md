# 5文字で改行するだけの話

## はじめに

`frame_text` は、長くなると読みにくい。

そこで `max_chars_per_line` ごとに分割する。

デフォルトは5文字。

## 実装の中心

`split_text_lines` は、入力文字列を指定文字数ごとに切る。

5文字なら、6文字目から次の行になる。

## 小さいけど大事

この処理があると、引数で行数を渡さなくてよくなる。

文字列と最大文字数だけで、行の形を決められる。

## まとめ

文字列と最大文字数だけで、行の形を決められる。
