# プロフィール画像の背景は白にする

プロフィール画像では、背景を透明にしない。

白い背景の上に `outer_text` と `inner_text` を描く。

## 透明が困る理由

見る場所によって背景色が変わると、文字の見え方も変わる。

特に淡い `inner_color` は、背景次第で読みにくくなる。

## 今回の判断

プロフィール用のアイコン画像と背景画像は白背景にする。

通常の合成用画像とは分けて考える。

