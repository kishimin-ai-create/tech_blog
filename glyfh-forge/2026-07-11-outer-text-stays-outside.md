# outer_textは外側だけに置く

`outer_text` は、`frame_text` の外側を埋めるための文字だ。

内側に入り込むと、`inner_text` が読みにくくなる。

## どう防ぐか

実装では、frameマスクを反転して外側マスクを作る。

その外側マスクに合う部分だけ、`outer_text` のアルファを残す。

つまり、`outer_text` は外側にしか存在できない。

## 大事な点

色を分けるだけでは足りない。

表示領域も分ける必要がある。

