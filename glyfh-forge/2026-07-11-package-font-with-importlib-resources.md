# フォントはパッケージリソースとして読む

## はじめに

画像生成では `ipaexg.ttf` を使う。

ファイルパスを直接決め打ちすると、wheelで配布したときに壊れやすい。

## 今回の読み方

`importlib.resources.files` で `glyph_forge.fonts` からフォントを探す。

これなら、パッケージ内のリソースとして扱える。

## 小さな教訓

開発中の相対パスで動いても、配布後に動くとは限らない。

## まとめ

開発中の相対パスで動いても、配布後に動くとは限らない。
